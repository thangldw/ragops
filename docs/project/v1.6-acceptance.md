# v1.6 pull-request adoption acceptance

## Decision

**GO and complete. The owner-authorized v1.6.0 GitHub release and PyPI
distribution are public. The reusable workflow and Trusted Publishing workflow
passed their GitHub-hosted runs.**

## Contract impact

The slice is additive. It introduces GitHub workflow adapters, print CSS, and
integration documentation. Scenario, trace, report, evaluator, metric, CLI,
API, and release-decision semantics do not change.

## Acceptance evidence

| Gate | Result | Evidence |
| --- | --- | --- |
| Lint and repository tests | Pass | Ruff and full pytest suite |
| Local PASS path | Pass | Baseline compared with itself exits 0 and writes Markdown |
| Local BLOCK path | Pass | Regressed candidate exits 2 and still writes Markdown |
| Trace tutorial path | Pass | Synthetic JSONL evaluates without credentials |
| Printable report | Pass | Generated HTML contains A4 print rules and case details |
| Reusable workflow structure | Pass | Read-only permissions, environment-indirected inputs, always-on evidence steps |
| GitHub-hosted workflow smoke | Pass | Run `29194652319` completed and uploaded `ragops-release-evidence` |
| OpenTelemetry adoption path | Pass | Synthetic spans convert to trace 0.4 and evaluate without credentials |
| PyPI workflow structure | Pass | Manual dispatch, tag/version check, OIDC permission, commit-pinned publish action |
| Package build | Pass | Wheel and sdist build from an isolated PEP 517 environment |
| Clean artifact install | Pass | Wheel and sdist install into new virtualenvs; version and credential-free demo verified |
| PyPI publication | Pass | Active OIDC publisher, public 1.6.0 wheel/sdist, clean install, and demo verification |

## Limitations

- The reusable workflow is included in v1.6.0 and passed run `29194652319`;
  production callers should pin the release tag or a reviewed commit.
- The workflow does not post PR comments and intentionally avoids write
  permissions or `pull_request_target` execution.
- Design-partner feedback and adoption/ROI remain external evidence.
