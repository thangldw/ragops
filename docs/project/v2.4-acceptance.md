# v2.4 adoption experience acceptance

## Decision

**RELEASED as 2.4.0.** The owner completed the project-scoped-token promotion on
2026-07-13. GitHub Release, GitHub Pages, and PyPI are public; the PyPI wheel and
source archive are byte-identical to the artifacts verified and published on
GitHub without GitHub Actions.

## Contract impact

Presentation-only and backwards compatible. No public evaluation contract,
schema, metric, gate, CLI/API, or open-core boundary changes.

## Acceptance evidence

| Gate | Result | Evidence |
| --- | --- | --- |
| Product requirement | Pass | Requirements v2.4 |
| Selected visual target | Pass | `docs/design-audit/adoption-refresh/source-option-3.png` |
| Desktop implementation | Pass | Interactive showcase captured at 1440×1024 |
| Primary interaction | Pass | Copy-command CTA tested; no browser console errors |
| Mobile implementation | Pass | Browser capture at 390×844; no horizontal overflow |
| Documentation consistency | Pass | Public adoption set uses one decision-first narrative |
| Core quality | Pass | Ruff clean; 144 release-environment tests pass; PASS fixture exits 0 and regression fixture blocks with exit 2 |
| Package integrity | Pass | Wheel and source archive built once, checksum-verified, and wheel clean-installed as 2.4.0 |
| GitHub Release | Pass | Public v2.4.0 release has wheel, source archive, SBOM, checksums, and local-release evidence |
| Public showcase | Pass | Legacy Pages build from `gh-pages` serves the v2.4 headline and install command without GitHub Actions |
| PyPI promotion | Pass | Public wheel SHA-256 `2d835970...99a8b7` and sdist SHA-256 `8b696660...f48a5c` match GitHub byte-for-byte; clean PyPI install reports 2.4.0 and completes `ragops demo` |

## Limitations

- Visual polish does not prove user acquisition or activation improvement.
- Historical technical records are intentionally not rewritten.
- Security testing remains deferred by owner instruction.
