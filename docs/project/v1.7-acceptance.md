# v1.7 broader-adoption acceptance

## Decision

**GO for release 1.7.0. The product owner explicitly authorized validation,
GitHub publication, and PyPI publication on 2026-07-13.**

## Contract impact

The slice is additive: one demo selection, one evaluator class, and two CLI
options. Existing default demo, scenario, trace, response, report, evaluator,
API, regression-policy, metric, and release-decision semantics remain
compatible.

## Acceptance evidence

| Gate | Result | Evidence |
| --- | --- | --- |
| Default demo compatibility | Pass | Omitted scenario still writes Japanese troubleshooting bundle |
| Support-triage demo | Pass | Baseline passes; unsafe candidate is blocked with named gates |
| Unicode boundary | Pass | Japanese answer equal to limit passes at four code points |
| Negative configuration | Pass | Zero, negative, and boolean limits are rejected |
| Diagnostic behavior | Pass | Over-budget finding is medium and does not silently block release |
| CLI integration | Pass | Configured limit appears in JSON evaluation evidence |
| GitHub discovery | Pass | Description, homepage, ten topics, and Discussions verified through the public API |
| Contributor entry points | Pass | Issues #1–#3 published with good-first/help-wanted labels |
| Proposal-review demo | Pass | Synthetic baseline passes; unsupported residency claim is blocked |
| GitLab CI adapter | Pass | MR-only recipe preserves CLI exit and always uploads evidence artifacts |
| PR-comment trust design | Pass | Read-only evaluation and future write publisher are explicitly separated |
| Core quality gate | Pass | Ruff passes; 84 tests pass on Python 3.12 and Python 3.13 |
| Release behavior | Pass | Benchmark baseline exits 0; known regression exits 2 with release BLOCK |
| Demo catalog | Pass | Default, support-triage, and proposal-review bundles generate end to end |
| Schemas | Pass | All seven published JSON schemas parse successfully |
| Package artifacts | Pass | Wheel and source distribution build as 1.7.0 and install cleanly |
| API compatibility | Pass | Clean wheel install returns API version 1.7.0 and evaluates the passing fixture |

## Limitations

- Both demos are synthetic and do not prove customer adoption or ROI.
- Code-point count is not token count, display width, or semantic quality.
- Enforcing length as a release gate requires a future reviewed policy
  contract; this release keeps it diagnostic.
- Python 3.11 is covered by repository CI; this local acceptance run directly
  exercised Python 3.12 and 3.13.
- The PR-comment publisher remains a reviewed design, not shipped write-enabled
  automation.
